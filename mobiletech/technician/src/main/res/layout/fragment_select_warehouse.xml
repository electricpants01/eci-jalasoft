<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="transferViewModel"
            type="eci.technician.activities.transfers.TransferViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".activities.transfers.SelectWarehouseFragment">

        <LinearLayout
            android:id="@+id/searchContainerLinearLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            android:orientation="vertical"
            app:layout_constraintTop_toBottomOf="@id/appbar">

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/searchContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:layout_marginBottom="8dp"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:theme="@style/Theme.MaterialComponents.Light"
                app:cardCornerRadius="10dp">

                <androidx.appcompat.widget.SearchView
                    android:id="@+id/searchView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/colorPrimaryDark"
                    app:iconifiedByDefault="false"
                    app:queryBackground="@null"
                    app:queryHint="Search..."
                    app:searchIcon="@drawable/search_white" />
            </com.google.android.material.card.MaterialCardView>
        </LinearLayout>

        <include
            android:id="@+id/empty_view"
            layout="@layout/view_empty_list"
            tools:visibility="gone" />

        <include
            android:id="@+id/progressBarContainer"
            layout="@layout/progress_bar_view"
            app:isVisible="@{transferViewModel.isLoading}"
            tools:visibility="gone" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/listWarehouses"
            android:visibility="@{transferViewModel.isLoading?View.GONE:View.VISIBLE}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </LinearLayout>
</layout>