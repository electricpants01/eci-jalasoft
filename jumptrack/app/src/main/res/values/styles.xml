<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="android:textColorHint">@color/text_color_hint</item>

        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="colorControlNormal">@color/login_control</item>

        <item name="android:dialogTheme">@style/MainDialogStyle</item>
        <item name="android:alertDialogTheme">@style/MainDialogStyle</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="colorControlNormal">@color/color_primary_dark</item>
        <item name="android:textColorSecondary">@color/color_primary_dark</item>
    </style>

    <!-- Main dialog style-->
    <style name="MainDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
    </style>

    <!-- Toolbar style-->
    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/color_primary_dark</item>
    </style>

    <style name="EditTextLoginStyle" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/login_control</item>
        <item name="editTextColor">?android:attr/textColorPrimary</item>
        <item name="android:editTextColor">?android:attr/textColorPrimary</item>
        <item name="android:textColor">?android:attr/textColorPrimary</item>
        <item name="android:textColorSecondary">?android:attr/textColorPrimary</item>
        <item name="android:hint">@color/login_hint</item>
        <item name="android:textColorHint">@color/login_hint</item>
    </style>

    <style name="generalCheckBoxStyle" parent="Theme.AppCompat.Light">
        <item name="android:textColor">?android:attr/textColorPrimary</item>
    </style>

    <!-- Dealer Logos -->
    <style name="dealerLogo">
        <item name="android:src">@drawable/jumptrack_logo</item>
    </style>

    <style name="dealerLogoLarge">
        <item name="android:src">@drawable/jumptrack_logo_large</item>
    </style>

    <!-- Fullscreen dialog style-->
    <style name="FullscreenAlertDialogStyle" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        // Set the splash screen background, animated icon, and animation duration.
        <item name="windowSplashScreenBackground">@color/iconColor</item>

        // Use windowSplashScreenAnimatedIcon to add either a drawable or an
        // animated drawable. One of these is required.
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">500</item>  # Required for
        # animated icons
        // Set the theme of the Activity that directly follows your splash screen.
        <item name="postSplashScreenTheme">@style/AppTheme</item>  # Required.
    </style>

</resources>
